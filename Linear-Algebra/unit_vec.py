# If a vector values are < 1 then it's scalar vector (αv) > 1.
# Zero vectors are not possible in unit vectors because this condiiton || αv || = 1
# If the vector values are > 1 the follow this condition: || αv || = (1 / ||v||) * ||v||

# Calc length of vector